# AI Agent 操作规则

> 本文件定义了AI在本项目中的操作边界。任何修改必须先读此文件。

## 一、禁止触碰的区域

以下文件和目录，AI严禁修改：

- `/router/*` —— 路由配置
- `/store/*` —— 全局状态管理（Pinia/Vuex）
- `/utils/request.ts` —— 请求拦截器
- `/types/*` —— 全局类型定义
- `vite.config.ts` / `vue.config.js` —— 构建配置

**例外情况**：如需修改上述文件，必须先向人类汇报原因，获得明确授权后方可操作。

## 二、每次修改的硬性约束

1. **单次PR不得超过300行**。超过则拆分为多个PR分批提交。
2. **新增依赖**（npm包）必须先告知，确认后再安装。
3. **API接口变更**必须同步更新接口文档（`/docs/api.md`）。
4. **删除任何代码**之前，先说明删除原因。

## 三、代码风格约束

- 使用 `<script setup>` 语法（Vue 3项目）
- 样式统一使用 `<style scoped>`，禁止内联 `:style`
- 禁止使用 `any` 类型
- 所有异步操作必须有错误处理（try-catch 或 .catch）
- 组件名使用 PascalCase，文件名使用 kebab-case

## 四、工作流程要求

1. **先出方案**：修改前先列出影响范围和修改计划，等待确认。
2. **测试先行**：新增功能必须同步编写单元测试。
3. **小步提交**：每完成一个独立功能点提交一次，不堆积。

## 五、否决权

人类有权否决任何AI提交的改动。被否决的代码需要说明原因，AI应记录并避免重复错误。